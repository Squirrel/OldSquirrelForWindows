<UserControl x:Class="Shimmer.WiXUi.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"  Background="White"
             d:DesignHeight="500" d:DesignWidth="350">

    <Grid Margin="0,16,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SampleUpdatingApp" FontWeight="ExtraLight" FontSize="32" Margin="8,0,0,0"/>
        <Border Grid.Row="1" Margin="0,16,0,0" Background="#efefef" BorderBrush="LightGray" BorderThickness="0,1,0,1">
            <TextBlock Text="Here is some sample test, this will be super long yadda yadda yadda" FontSize="14" TextWrapping="WrapWithOverflow" Margin="16" />
        </Border>

        <TextBlock Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontWeight="Light" FontSize="36" Margin="0,8,0,0">
            <Hyperlink>Install</Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
